{
  "title": "Web: scrape-job polling implemented three different ways — extract \
one pollScrapeJob util",
  "description": "The 'poll a scrape job until completed/failed/timeout' logic \
is reinvented three times in ramekin-ui with different timeout/backoff \
behavior:\n\
\n\
- pages/ScrapeStatusPage.tsx:78-128 — setTimeout + tick interval\n\
- pages/ImportPage.tsx:65-70 — async delay loop with JOB_POLL_INTERVAL_MS\n\
- pages/ViewRecipePage.tsx:551-573 — recursive poll() with a 120s deadline\n\
\n\
Extract a shared pollScrapeJob(api, jobId, opts) util returning a terminal \
result, and converge the three call sites on one timeout/backoff policy.\n\
\n\
Web-only: iOS drives scrape status through its own store; if its polling \
policy differs materially, note it in the PR, but no shared vectors needed (no \
deterministic formatting logic involved).",
  "status": "open",
  "priority": 4,
  "type": "task",
  "labels": [
    "web",
  ],
  "created_at": "2026-07-02T05:30:02-07:00",
  "updated_at": "2026-07-02T05:30:02-07:00",
}
